<%= render 'layouts/list', book: @book, user: current_user %>


<% @books.each do |book| %>
<div>
  <tr>

          <td><%= link_to book.title, book_path(book.id) %></td>
          <td><%= book.body %></td>
  </tr>
</div>
<% end %>