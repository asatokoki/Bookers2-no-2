<h2>User info</h2>

<table class="table">
  <tbody>
    <tr>
      <td><%= attachment_image_tag user, :profile_image, size: "50x50", fallback: "img_7c62aaccd6714ba4a1fedfaf0d5702c3142297.jpg" %></td>

    </tr>

    <tr>
      <th><p>name</p></th>
      <th><%= user.name %></th>
    </tr>

    <tr>
      <th><p>introduction</p></th>
      <th><%= user.introduction %></th>
     </tr>
  </tbody>
</table>

<div class="row">
    <%=link_to '<i class="fab fa-accessible-icon"></i>', edit_user_path(user.id) , class:'btn btn-light'%>
</div>


<h2 class="mt-3">New book</h2>
<%= form_with model: book, local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>
